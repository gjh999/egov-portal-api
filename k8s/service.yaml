# 프론트 파드가 이 이름으로 백엔드를 찾는다(BACKEND_URL=http://egov-portal-api:18090).
apiVersion: v1
kind: Service
metadata:
  name: egov-portal-api
  labels:
    app: egov-portal-api
spec:
  type: ClusterIP
  selector:
    app: egov-portal-api
  ports:
    - name: http
      port: 18090
      targetPort: http
